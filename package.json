{
  "name": "@jedavard/rabbitmq-amqplib-wrapper",
  "version": "0.1.0",
  "description": "Event subscriber and publisher on NATS for kubernetes microservices",
  "main": "./build/index.js",
  "types": "./build/index.d.js",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish",
    "example:client": "ts-node-dev examples/client.ts",
    "example:server": "ts-node-dev examples/server.ts"
  },
  "keywords": ["rabbitmq","rabbit","amqplib","rabbitmq client"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.8",
    "@types/jest": "^26.0.14",
    "@types/amqplib": "^0.5.17",
    "ts-node-dev": "^1.1.1",
    "jest": "^26.5.2",
    "prettier": "^2.2.1",
    "del-cli": "^3.0.1"
  },
  "dependencies": {
    "amqplib": "^0.6.0",
    "dotenv": "^8.2.0",
    "typescript": "^4.1.3"
  }
}
